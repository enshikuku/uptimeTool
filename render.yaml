# render.yaml
services:
  - type: web
    name: uptime-tool
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production